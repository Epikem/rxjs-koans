import { Main } from "./main";

const ___ = "fill this with correct answer";
const ____ = 0;

export default function (done: Mocha.Done) {
    console.log('started');

    const main = new Main();
    main.run();
}
